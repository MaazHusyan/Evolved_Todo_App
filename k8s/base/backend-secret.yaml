apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  labels:
    app: evolve-todo
    component: backend
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values with actual secrets
  # For production, use sealed-secrets, external-secrets, or vault
  database-url: "postgresql://user:password@postgres:5432/evolve_todo"
  jwt-secret: "CHANGE_ME_TO_RANDOM_SECRET_KEY"
  openai-api-key: ""
  groq-api-key: ""
