apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  labels:
    app: evolve-todo
    component: backend
data:
  environment: "production"
  log-level: "info"
  cors-origins: "http://localhost:3000,http://frontend:3000"
