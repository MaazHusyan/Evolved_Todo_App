Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

Application components:
- Backend API: {{ include "evolve-todo.fullname" . }}-backend
- Frontend UI: {{ include "evolve-todo.fullname" . }}-frontend

{{- if .Values.ingress.enabled }}

The application is accessible at:
  http://{{ .Values.ingress.host }}

Make sure to add the following to your /etc/hosts file for local development:
  127.0.0.1 {{ .Values.ingress.host }}

{{- else }}

To access the application, you can port-forward:

  Backend:
  $ kubectl port-forward -n {{ .Values.namespace }} svc/{{ include "evolve-todo.fullname" . }}-backend {{ .Values.backend.service.port }}:{{ .Values.backend.service.port }}

  Frontend:
  $ kubectl port-forward -n {{ .Values.namespace }} svc/{{ include "evolve-todo.fullname" . }}-frontend {{ .Values.frontend.service.port }}:{{ .Values.frontend.service.port }}

{{- end }}

IMPORTANT SECURITY NOTES:
1. Update the secrets in values.yaml with actual production values
2. Consider using sealed-secrets or external-secrets for production
3. Update the JWT_SECRET to a strong random value
4. Configure proper database credentials

Health check endpoints:
- Backend liveness: http://{{ .Values.ingress.host }}/api/health/live
- Backend readiness: http://{{ .Values.ingress.host }}/api/health/ready

For more information, visit: {{ .Chart.Home }}
